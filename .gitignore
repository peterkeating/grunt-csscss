# ignoring node modules, these should be retrieved using 'npm install' command.
node_modules

# node logging.
npm-debug.log

# directory creating by the Sass conversion which is done when testing grunt task
# against Compass stylesheets.
.sass-cache/

# convenient directory for storing files that should be ignored by Git.
ignore/
